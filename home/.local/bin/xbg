#!/usr/bin/env bash
# 更新系统 + Helium
set -euo pipefail
sudo xbps-install -Syu
repo=${VOIDLINUX_CONFIG:-$HOME/voidlinux-config}
if [ -f "$repo/90-helium.sh" ]; then
	sh "$repo/90-helium.sh"
else
	echo "跳过 Helium：找不到 $repo/90-helium.sh" >&2
fi
