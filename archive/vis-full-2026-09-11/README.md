# Full Vis configuration before single-file simplification

Copied byte-for-byte from `root/home/.config/vis` at commit `a283e94`.
Includes project navigation, cross-file completion/history, cursor state,
lock notifications, all themes, and the already-commented note integration.
No personal notes or runtime data are included.

To restore, replace the repository's `root/home/.config/vis` directory with
this `vis` directory; keep `~/.config/vis` linked to the active repository path.
